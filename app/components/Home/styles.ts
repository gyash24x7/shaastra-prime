import { StyleSheet } from "react-native";

export default StyleSheet.create({
	profileCard: {
		display: "flex",
		justifyContent: "space-between",
		width: "100%",
		borderWidth: 2,
		borderColor: "#303030",
		flex: 1
	},

	detailsList: {
		height: "100%"
	}
});
