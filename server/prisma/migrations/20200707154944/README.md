# Migration `20200707154944`

This migration has been generated by gyash24x7 at 7/7/2020, 3:49:44 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "UserRole" AS ENUM ('COORD', 'HEAD', 'CORE', 'COCAS');

CREATE TABLE "public"."User" (
"about" text  NOT NULL DEFAULT E'',"department" text  NOT NULL ,"email" text  NOT NULL ,"id" text  NOT NULL ,"mobile" text  NOT NULL ,"name" text  NOT NULL ,"password" text  NOT NULL ,"passwordOTP" text   ,"role" "UserRole" NOT NULL DEFAULT E'COORD',"rollNumber" text  NOT NULL ,"salt" text  NOT NULL ,"upi" text  NOT NULL DEFAULT E'',"verificationOTP" text  NOT NULL ,"verified" boolean  NOT NULL DEFAULT false,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "User.salt" ON "public"."User"("salt")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200707154944
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,35 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id              String   @default(cuid()) @id
+  name            String
+  email           String   @unique
+  rollNumber      String
+  password        String
+  salt            String   @unique
+  role            UserRole @default(COORD)
+  mobile          String
+  upi             String   @default("")
+  about           String   @default("")
+  verified        Boolean  @default(false)
+  verificationOTP String
+  passwordOTP     String?
+  department      String
+}
+
+enum UserRole {
+  COORD
+  HEAD
+  CORE
+  COCAS
+}
```


