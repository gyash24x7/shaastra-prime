{
	"name": "shaastra-prime",
	"version": "1.0.0",
	"main": "index.js",
	"repository": "https://github.com/gyash24x7/shaastra-prime.git",
	"license": "MIT",
	"private": true,
	"workspaces": [
		"./server",
		"./web"
	],
	"scripts": {
		"dev:web": "yarn workspace web dev",
		"dev:server": "yarn workspace server dev",
		"build:web": "yarn workspace web build",
		"gen:client": "yarn workspace web gen",
		"gen:server": "yarn workspace server gen",
		"db:migrate": "cd server && npx prisma migrate save --experimental && npx prisma migrate up --experimental && cd ..",
		"db:studio": "cd server && npx prisma studio --experimental"
	}
}
